# Minecraft 26.2 is fully unobfuscated (Mojang official names) -- no mappings
# block at all is possible or needed. Fabric stopped publishing Yarn mappings
# past the 1.21.x line, so there is no yarn_mappings key here (matches the
# comment already recorded on this decision in the sibling repo FlightHud,
# which completed this same version).
loader_version=0.19.3

# Fabric API
fabric_version=0.156.0+26.2

# NeoForge
neoforge_version=26.2.0.58
